build:
  env:
    NODE_ENV: production
  steps:
    - run: npm install
    - run: npm run build
    - run: echo 'gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;' >> public/.htaccess
  output: .